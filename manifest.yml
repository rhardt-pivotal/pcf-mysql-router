---
applications:
 - name: mysqlrouter
   memory: 1G
   instances: 1
   buildpacks:
    - binary_buildpack
   stack: cflinuxfs3
   path: .
   command: echo 'your-password' | PATH=./bin LD_LIBRARY_PATH=./lib mysqlrouter --bootstrap yokim@mysql-db.eastus2.cloudapp.azure.com:3306 --directory ./current --conf-base-port=8080 && PATH=./bin LD_LIBRARY_PATH=./lib mysqlrouter -c ./current/mysqlrouter.conf &
